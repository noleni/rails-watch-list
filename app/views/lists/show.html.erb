

<div class="container">
  <h2><%= @list.name %></h2>
  <%= cl_image_tag @list.photo.key, height: 300, width: 400, crop: :fill %>
  <div class="row">
    <% @list.bookmarks.each do |bookmark| %>
      <div class="col-6">
        <div class="card-product">
         <img src=<%= bookmark.movie.poster_url %>>
         <h2><%= bookmark.movie.title %></h2>
         <p class="card-product-infos"><%= bookmark.comment %></p>
         <%= bookmark.movie.rating %>
          <%= link_to "Delete",
          bookmark,
          data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
        </div>
      </div>
    <% end %>
  </div>

    <%= link_to "Add a comment", new_list_bookmark_path(@list), class:"btn btn-primary mt-3" %>
    <%= link_to 'Retour à la home', lists_path, class:"btn btn-primary mt-3" %>
</div>





<%# <div class="container">
  <h1>Add a movie to this list</h1>
  <%= simple_form_for [@bookmark, @list] do |f| %>
    <%# <%= f.association :movie %>
    <%# <%= f.submit "Submit review", class: "btn btn-primary" %>
  <%# <% end %>
<%# </div> %>
